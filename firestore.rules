rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pages/{pageId} {
      allow read: if true; 
      allow write: if request.auth != null; 

     
      match /cards/{cardId} {
        allow read: if true;
        allow write: if request.auth != null;
      }
    }
  }
}